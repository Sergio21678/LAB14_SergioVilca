name: Build and Deploy .NET Application

on:
  push:
    branches:
      - main # Automáticamente se ejecuta cuando se suben cambios a la rama principal

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Check out code
        uses: actions/checkout@v2

      - name: Set up .NET
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: '9.0.x' # Ajustado a 9.0 para coincidir con tu proyecto

      - name: Restore dependencies
        run: dotnet restore

      - name: Build application
        run: dotnet build --configuration Release

      - name: Publish application
        run: dotnet publish --configuration Release --output ./published

      # Nota: Este último paso es solo un ejemplo del laboratorio. 
      # Fallará en GitHub porque no tienes configurado un servidor real para hacer 'scp'.
      # Lo dejo comentado para que no rompa tu proceso de construcción.
      - name: Deploy to server (ejemplo de despliegue)
        run: |
          echo "Simulando despliegue..."
          # scp -r ./published username@server:/path/to/deploy